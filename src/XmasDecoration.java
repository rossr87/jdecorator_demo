
public abstract class XmasDecoration extends Plain {
	/*
	 * Notice that even though this method has an implementation in
	 * the superclass, we're overriding it with an abstract method.
	 */
	public abstract String get_name();
}
